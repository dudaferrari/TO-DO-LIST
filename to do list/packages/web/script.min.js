const API_URL = "http://localhost:4000/api/todos", form = document.getElementById("todo-form"), input = document.getElementById("new-todo"), descriptionInput = document.getElementById("new-description"), reminderInput = document.getElementById("new-reminder"), list = document.getElementById("todo-list"), statusMessage = document.getElementById("status-message"), taskCount = document.getElementById("task-count"), emptyState = document.getElementById("empty-state"), refreshButton = document.getElementById("refresh-button"); let todos = []; function showNotice(e, t = "info") { statusMessage.textContent = e, statusMessage.style.color = "error" === t ? "#b91c1c" : "#2563eb" } function clearNotice() { statusMessage.textContent = "" } function formatReminder(e) { if (!e) return ""; const t = new Date(e); return t.toLocaleString("pt-BR", { dateStyle: "short", timeStyle: "short" }) } function setTaskCount(e) { taskCount.textContent = e } function renderTodos() { list.innerHTML = "", 0 === todos.length ? (emptyState.style.display = "block", setTaskCount(0), void 0) : (emptyState.style.display = "none", setTaskCount(todos.length), todos.forEach(e => { const t = document.createElement("li"); t.className = `task-item${e.completed ? " done" : ""}`; const n = document.createElement("input"); n.type = "checkbox", n.checked = e.completed, n.className = "task-checkbox", n.addEventListener("change", () => toggleTodo(e.id)), t.appendChild(n); const o = document.createElement("div"); o.className = "task-info"; const r = document.createElement("strong"); r.textContent = e.title, o.appendChild(r), e.description && (function (e) { const t = document.createElement("div"); t.textContent = e, t.style.color = "#475569", o.appendChild(t) }(e.description)), e.reminder && (function (e) { const t = document.createElement("small"); t.textContent = `Lembrete: ${formatReminder(e)}`, o.appendChild(t) }(e.reminder)); t.appendChild(o); const a = document.createElement("div"); a.className = "task-actions"; const l = document.createElement("button"); l.type = "button", l.textContent = "Editar lembrete", l.addEventListener("click", async () => { const t = prompt("Nova data/hora (YYYY-MM-DDTHH:MM)", e.reminder || ""); if (null === t) return; await updateReminder(e.id, t.trim() || null) }), a.appendChild(l); const c = document.createElement("button"); c.type = "button", c.textContent = "Remover", c.addEventListener("click", async () => { await removeTodo(e.id) }), a.appendChild(c), t.appendChild(a), list.appendChild(t) })) } async function fetchTodos() { try { clearNotice(); const e = await fetch(API_URL); if (!e.ok) throw new Error("Não foi possível carregar as tarefas."); todos = await e.json(), renderTodos() } catch (e) { showNotice("Erro: certifique-se de que a API está rodando em http://localhost:4000", "error") } } async function addTodo(e, t) { const n = e.trim(); if (!n) { showNotice("Escreva uma tarefa antes de adicionar.", "error"); return } try { clearNotice(); const o = { title: n }; const desc = descriptionInput.value && descriptionInput.value.trim(); if (desc) o.description = desc; t && (o.reminder = new Date(t).toISOString()); const r = await fetch(API_URL, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(o) }); if (!r.ok) throw new Error("Falha ao criar tarefa."); input.value = "", descriptionInput.value = "", reminderInput.value = "", showNotice("Tarefa adicionada com sucesso!"), await fetchTodos() } catch (e) { showNotice("Erro ao adicionar tarefa. Verifique a API.", "error") } } async function toggleTodo(e) { try { clearNotice(); const t = await fetch(`${API_URL}/${e}/toggle`, { method: "PATCH" }); if (!t.ok) throw new Error("Falha ao atualizar tarefa."); await fetchTodos() } catch (e) { showNotice("Erro ao atualizar tarefa. Verifique a API.", "error") } } async function removeTodo(e) { try { clearNotice(); const t = await fetch(`${API_URL}/${e}`, { method: "DELETE" }); if (!t.ok) throw new Error("Falha ao remover tarefa."); await fetchTodos() } catch (e) { showNotice("Erro ao remover tarefa. Verifique a API.", "error") } } async function updateReminder(e, t) { try { clearNotice(); const n = await fetch(`${API_URL}/${e}/reminder`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ reminder: t || null }) }); if (!n.ok) throw new Error("Falha ao atualizar lembrete."); await fetchTodos() } catch (e) { showNotice("Erro ao atualizar lembrete. Verifique a API.", "error") } } form.addEventListener("submit", async e => { e.preventDefault(), await addTodo(input.value, reminderInput.value) }), refreshButton.addEventListener("click", fetchTodos), fetchTodos();